## [hover-fix] className-driven transitions get stuck at progress 0 on this page; inline-style-driven transitions work. Converted ring/glow/fill/label/tooltip hover to inline styles (opacity, scale, backgroundColor, boxShadow, fontSize, fontWeight, letterSpacing, color).
## [lag-fix] CSS transitions STALL when the browser throttles frames (idle page, background tab, slow device, headless). Hover took ~1780ms → felt "sumamente lagueado". Root cause: transitions need compositor frames to progress; throttled environments produce none. FIX: removed the 5 hover transition classes (ring/glow/fill/label/tooltip) → hover applies INSTANTLY (43ms verified). The ~1.4s delay in headless was frame-throttling, not a code bug. Keep transitions ONLY where they run during page-load activity (node div fade-in works).
## [final-wave] F1 APPROVE (no zoom/pan, margins 60, no overflow-hidden, build passes). F2 APPROVE (sim cools to full stop: alphaTarget(0), alphaMin(0.5), applySize guarded by appliedSizeRef, tick handler never restarts sim).
